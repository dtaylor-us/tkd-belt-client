import { BeltColorDirective } from './belt-color.directive';

describe('BeltColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new BeltColorDirective();
    // expect(directive).toBeTruthy();
  });
});
